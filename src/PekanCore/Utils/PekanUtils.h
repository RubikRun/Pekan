#pragma once

#include <string>

namespace Pekan
{
namespace Utils
{

    // Reads a text file's contents into a string
    std::string readFileToString(const char* filePath);

} // namespace Utils
} // namespace Pekan
